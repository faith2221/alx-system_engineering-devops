#!/usr/bin/env bash
# Debugging nginx.
# Service (init) must say that ngix is not running.
sed -i 's/8080/80/g' /etc/nginx/sites-enabled/default

# Restart anaginx
service ngix restart

# Terminates the first ngix proce=ss ID
pkill -o nginx
